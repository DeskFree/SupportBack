pipeline{
    agent any
    stages{
        stage('Build'){
            steps{
                sh 'npm run build'
            }
            post {
                success {
                    archiveArtifacts artifacts: 'dist/**'
                }
            }
        }
    }
}